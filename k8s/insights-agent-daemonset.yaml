# Create namespace
apiVersion: v1
kind: Namespace
metadata:
  name: postman-insights-namespace
---
# Create service account
apiVersion: v1
kind: ServiceAccount
metadata:
  name: postman-insights-service-account
  namespace: postman-insights-namespace
automountServiceAccountToken: false
---
# Create role
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: postman-insights-read-only-role
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "watch", "list"]
---
# Give viewer access to the service account
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: postman-insights-view-all-resources-binding
subjects:
- kind: ServiceAccount
  name: postman-insights-service-account
  namespace: postman-insights-namespace
roleRef:
  kind: ClusterRole
  name: postman-insights-read-only-role
  apiGroup: rbac.authorization.k8s.io
---
# Create daemonset for running agent (Discovery Mode)
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: postman-insights-agent
  namespace: postman-insights-namespace
spec:
  selector:
    matchLabels:
      name: postman-insights-agent
  template:
    metadata:
      labels:
        name: postman-insights-agent
    spec:
      serviceAccountName: postman-insights-service-account
      automountServiceAccountToken: true
      hostPID: true
      containers:
      - name: postman-insights-agent
        image: public.ecr.aws/postman/postman-insights-agent:latest
        imagePullPolicy: Always
        args:
        - kube
        - run
        - --discovery-mode
        - --repro-mode
        - --debug
        - --include-namespaces
        - jade-global
        env:
        - name: POSTMAN_INSIGHTS_CLUSTER_NAME
          value: "jade-global-cluster"
        - name: POSTMAN_INSIGHTS_API_KEY
          valueFrom:
            secretKeyRef:
              name: postman-agent-secrets
              key: postman-api-key
        - name: POSTMAN_INSIGHTS_WORKSPACE_ID
          value: "26eadf3c-acc8-40d3-aa89-b5df9f5af6af"
        - name: POSTMAN_INSIGHTS_VERIFICATION_TOKEN
          value: "tvt_2A98gFU0j1F87P04RPEitH"
        - name: POSTMAN_INSIGHTS_K8S_NODE
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: POSTMAN_INSIGHTS_CRI_ENDPOINT
          value: /var/run/containerd/containerd.sock
        - name: POSTMAN_INSIGHTS_SYSTEM_ENV
          value: "936898ce-cf73-4feb-827e-8a0e9d5e695e"
        resources:
          requests:
            cpu: 200m
            memory: 500Mi
          limits:
            cpu: "1"
            memory: 3Gi
        securityContext:
          capabilities:
            add:
              - NET_RAW
              - SYS_ADMIN
              - SYS_PTRACE
            drop:
              - ALL
        volumeMounts:
        - mountPath: /host/proc
          name: host-procfs
          readOnly: true
        - mountPath: /host/var/run/netns
          mountPropagation: HostToContainer
          name: host-netns
          readOnly: true
        - mountPath: /var/run/containerd/containerd.sock
          name: containerdsocket
          readOnly: true
      restartPolicy: Always
      volumes:
      - name: host-procfs
        hostPath:
          path: /proc
          type: Directory
      - name: host-netns
        hostPath:
          path: /var/run/netns
          type: Directory
      - name: containerdsocket
        hostPath:
          path: /var/run/containerd/containerd.sock
          type: Socket
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 30%
